pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: bunx @biomejs/biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bunx tsc --noEmit
