# Public
VITE_PUBLIC_BASE_URL="http://localhost:3000"
VITE_PUBLIC_OPENEXCHANGERATES_API_KEY=""

# Auth
AUTH_URL="http://localhost:3000/api/auth"
AUTH_SECRET=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
AUTH_ADMIN_EMAILS="your-email@example.com,another-email@example.com"

# Database
# Local development uses Wrangler's local D1 state via:
# bun run db:init:local
#
# Production uses the remote Cloudflare D1 binding configured in wrangler.json via:
# bun run db:migrate:remote
